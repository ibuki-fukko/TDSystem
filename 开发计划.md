# **三周开发详细任务分解（功能仅供参考，需根据原有的系统调整）**
# **各模块负责人可根据实际情况调整任务内容与分配**

## **第一周：基础框架与核心模块 （1月28日--2月3日）**

### **前端任务清单（Vue 3 + Element Plus）**
```
✅ Day 1-2: 项目初始化与基础架构
  - 使用Vite创建Vue 3项目
  - 集成Element Plus UI组件库并配置按需导入
  - 配置Vue Router路由系统
  - 配置Pinia状态管理
  - 配置Axios请求拦截和响应处理
  - 搭建基础Layout布局（头部导航、侧边栏、主内容区）
  - 配置环境变量（开发/生产）

✅ Day 3-4: 用户认证模块
  - 登录页面实现（SSO跳转和本地登录表单）
  - SSO回调处理页面
  - 用户信息展示组件
  - 动态路由菜单（基于角色权限生成）
  - 路由权限守卫（未登录跳转、角色权限校验）
  - Token存储和刷新机制

✅ Day 5-6: 答辩批次管理（管理员端）
  - 批次列表页面（使用Element Plus Table组件）
  - 创建/编辑批次弹窗表单
  - 批次状态切换组件（开关控制）
  - 批次详情页面
  - 搜索、筛选、分页功能

✅ Day 7: 学生报名功能
  - 学生端可报名批次列表
  - 报名申请表单（使用Element Plus Form组件）
  - 我的报名记录页面
  - 取消报名功能（在允许时间内）
```

### **后端任务清单（Spring Boot + MyBatis Plus）（1月28日--2月3日）**
```
✅ Day 1-2: 项目初始化与数据库设计
  - 创建Spring Boot 2.7+项目
  - 配置MySQL + MyBatis Plus + 代码生成器
  - 配置Redis（Spring Data Redis）
  - 设计并创建核心数据库表：
    * sys_user（用户表）
    * sys_role（角色表）
    * sys_user_role（用户角色关联表）
    * defense_batch（答辩批次表）
    * defense_application（报名表）
  - 使用Lombok简化实体类
  - 配置Swagger 3.0接口文档

✅ Day 3-4: 用户认证与权限模块
  - 集成Spring Security + JWT
  - 实现JWT令牌生成、验证和刷新
  - 自定义UserDetailsService
  - 实现认证过滤器（JwtAuthenticationFilter）
  - 配置Security权限控制（@PreAuthorize）
  - 用户管理CRUD接口（使用MyBatis Plus Service）

✅ Day 5-6: 答辩批次管理接口
  - 答辩批次CRUD接口（使用MyBatis Plus分页）
  - 批次状态修改接口（开放/关闭报名）
  - 获取开放报名批次接口
  - 接口参数校验（@Validated）
  - 统一响应结果封装（Result）

✅ Day 7: 报名管理接口
  - 学生报名接口（校验是否已报名、批次状态）
  - 学生查看报名记录接口
  - 管理员查看报名列表接口
  - 取消报名接口
  - 事务管理（@Transactional）
```

### **第一周交付物（1月28日--2月3日）**
1. ✅ 基于Vite + Vue 3的前端项目框架
2. ✅ Spring Boot + Spring Security + JWT后端认证框架
3. ✅ 完整的数据库表结构（基础表）
4. ✅ 管理员可创建和管理答辩批次
5. ✅ 学生可查看和报名开放批次
5. ✅ 熟悉开发流程，完成基础模块的开发
---

## **第二周：核心业务流程（2月4日--2月10日）**

### **前端任务清单**
```
✅ Day 8-9: 论文上传模块
  - 使用Element Plus Upload组件实现文件上传
  - 集成PDF.js实现论文在线预览
  - 论文类型选择器（盲审/查重/终版）
  - 我的论文列表页面
  - 文件上传进度展示
  - 论文状态标签组件

✅ Day 10-11: 盲审分配模块（管理员）
  - 待分配学生列表页面
  - 教师选择器组件（学院/企业教师分类）
  - 手动分配界面（支持拖拽分配）
  - 分配结果预览
  - 使用Pinia管理分配状态

✅ Day 12-13: 盲审评审模块（教师端）
  - 我的盲审任务列表
  - 评审页面（集成PDF.js预览匿名论文）
  - 评分表组件（使用Element Plus Form）
  - 富文本编辑器（使用Vue-Quill）
  - 评审结果提交确认

✅ Day 14: 答辩分组模块（基础）
  - 分组列表页面
  - 手动创建分组表单
  - 分组详情展示组件
  - 成员管理界面（添加/移除学生教师）
```

### **后端任务清单（2月4日--2月10日）**
```
✅ Day 8-9: 论文上传与管理
  - 集成MinIO对象存储服务
  - 实现文件上传接口（支持分片上传）
  - 论文信息表设计：
    * paper_info（论文信息表）
    * paper_file（论文文件表，关联MinIO）
  - 论文CRUD接口（使用MyBatis Plus）
  - 论文下载接口（权限控制，盲审论文匿名）

✅ Day 10-11: 盲审分配系统
  - 盲审分配表设计：
    * review_assignment（盲审分配表）
    * review_task（评审任务表）
  - 手动分配接口（为每个学生分配两位评审教师）
  - 教师任务查询接口（使用MyBatis Plus关联查询）
  - 分配记录管理接口

✅ Day 12-13: 盲审评审功能
  - 评审结果表设计：
    * review_result（评审结果表）
  - 提交评审接口（包含评分和评语）
  - 获取匿名论文接口（去除学生个人信息）
  - 学生查看评审结果接口（不显示教师姓名）

✅ Day 14: 答辩分组基础
  - 分组表结构设计：
    * defense_group（答辩分组表）
    * group_member（分组成员表）
    * student_supervisor（学生-导师关系表，关键）
  - 分组CRUD接口
  - 成员管理接口
  - 分组查询接口
```

### **第二周交付物（2月4日--2月10日）**
1. ✅ 完整的论文上传、预览、下载功能（MinIO + PDF.js）
2. ✅ 盲审分配系统（管理员手动分配）
3. ✅ 教师在线评审功能
4. ✅ 学生查看评审结果（匿名）
5. ✅ 答辩分组基础管理

---

## **第三周：高级功能与系统集成（2月11日--3月1日）**

### **前端任务清单**
```
✅ Day 15-16: 答辩分组高级功能
  - 自动分组参数设置页面
  - 分组编辑页面（添加/移除成员）
  - 答辩主任设置组件
  - 时间地点设置表单（教秘功能）
  - 集成ECharts展示分组统计
  - Excel导出功能（使用EasyExcel前端配合）

✅ Day 17-18: 答辩结果管理
  - 答辩评分录入页面（按分组）
  - 评分表组件（带实时计算）
  - 答辩结果列表页面
  - 二辩标记功能
  - 最终成绩查看页面（学生端）

✅ Day 19-20: 系统集成与优化
  - 仪表盘页面（使用ECharts展示数据统计）
  - 批量导入用户页面（Excel上传）
  - 通知消息组件
  - 响应式布局优化
  - 表单验证完善（Element Plus Form验证）
  - 性能优化（路由懒加载、组件懒加载）

✅ Day 21: 测试与部署
  - 端到端流程测试
  - 权限验证测试
  - API接口联调测试
  - 构建优化（Vite构建配置）
  - 部署文档编写
```

### **后端任务清单（2月11日--3月1日）**
```
✅ Day 15-16: 自动分组算法
  - 自动分组接口（POST /api/groups/auto-generate）
  - 分组算法实现（使用Java Stream API）
  - 分组约束条件：
    * 避开学生自己的导师（查询student_supervisor表）
    * 每组至少包含两名盲审教师
    * 平衡教师工作量
  - 分组结果预览接口

✅ Day 17-18: 答辩评分与结果
  - 答辩评分表设计：
    * defense_score（答辩评分表）
    * defense_result（答辩结果表）
  - 提交评分接口（事务处理）
  - 答辩结果录入接口
  - 二辩标记接口
  - 成绩计算逻辑（综合盲审和答辩成绩）

✅ Day 19-20: 高级功能与集成
  - 导出功能接口（使用EasyExcel导出Excel）
  - 数据统计接口（使用ECharts所需格式）
  - 批量导入接口（EasyExcel解析Excel）
  - Redis缓存集成（缓存用户信息、配置信息）
  - 系统配置表设计（sys_config）
  - 定时任务（@Scheduled，处理超时任务）

✅ Day 21: 系统测试与部署
  - 接口压力测试（JMeter）
  - 数据库索引优化
  - SQL性能分析（使用MyBatis Plus性能分析插件）
  - 日志系统完善（Logback + SLF4J）
  - 生产环境配置（多环境配置）
  - Docker容器化部署准备
```

### **技术栈对应开发细节（举例）**

#### **前端关键技术实现**
```javascript
// 1. PDF预览组件
import { pdfjs } from 'pdfjs-dist';

// 2. 文件上传组件
<el-upload
  :action="uploadUrl"
  :headers="{ Authorization: `Bearer ${token}` }"
  :on-progress="handleProgress"
>

// 3. ECharts集成
import * as echarts from 'echarts';

// 4. Excel导出
import * as XLSX from 'xlsx';
```

#### **后端关键技术配置（举例）**
```yaml
# application.yml 关键配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/defense_system
  redis:
    host: localhost
    port: 6379
  security:
    jwt:
      secret: your-secret-key
      expiration: 86400000

minio:
  endpoint: http://localhost:9000
  accessKey: minioadmin
  secretKey: minioadmin
  bucket: defense-papers
```

#### **MyBatis Plus配置**
```java
@Configuration
@MapperScan("com.defense.mapper")
public class MybatisPlusConfig {
    
    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        // 分页插件
        interceptor.addInnerInterceptor(new PaginationInnerInterceptor());
        // 乐观锁插件
        interceptor.addInnerInterceptor(new OptimisticLockerInnerInterceptor());
        return interceptor;
    }
}
```

#### **Spring Security + JWT配置**
```java
@Configuration
@EnableWebSecurity
@EnableGlobalMethodSecurity(prePostEnabled = true)
public class SecurityConfig {
    
    @Bean
    public JwtAuthenticationFilter jwtAuthenticationFilter() {
        return new JwtAuthenticationFilter();
    }
    
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

### **第三周交付物（2月11日--3月1日）**
1. ✅ 完整的自动分组功能（基于规则的算法）
2. ✅ 答辩评分和结果管理系统
3. ✅ 二辩流程支持
4. ✅ 数据导出（EasyExcel）和统计图表（ECharts）
5. ✅ 完整的端到端流程
6. ✅ 生产环境部署配置



## **🎯 风险控制与应急预案**

1. **技术风险**：
   - MinIO集成问题 → 备用方案：本地文件存储
   - SSO对接延迟 → 先实现本地认证，后期集成

2. **时间风险**：
   - 自动分组算法复杂 → 简化规则，先实现手动分组
   - 文件上传性能问题 → 采用分片上传，前端压缩

3. **人员风险**：
   - 前后端沟通不畅 → 多沟通开会，API文档先行
   - 开发进度不一致 → 接口Mock数据先行开发


## **🎯 以上内容仅供参考，要根据具体系统要求和开发进度调整！！！**